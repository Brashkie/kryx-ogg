[package]
name = "ogg-core"
description = "Pure-Rust Ogg container reader — the engine behind @kryxjs/ogg."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lib]
path = "src/lib.rs"

# Zero dependencies — this is the point.
[dependencies]
